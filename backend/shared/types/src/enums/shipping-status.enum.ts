export enum ShippingStatus {
  CREATED = 'created',
  RESERVED = 'reserved',
  IN_TRANSIT = 'in_transit',
  ARRIVED = 'arrived',
  IN_DISTRIBUTION = 'in_distribution',
  DELIVERED = 'delivered',
  CANCELLED = 'cancelled',
}
