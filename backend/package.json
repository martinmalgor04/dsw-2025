{
  "name": "logistica-grupo12-tpi",
  "version": "1.0.0",
  "description": "Microservicio de Logística y Transporte - TPI Desarrollo de Software 2025 - UTN FRRE - Grupo 12",
  "author": "Grupo 12 - UTN FRRE",
  "private": true,
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/FRRe-DS/2025-12-TPI.git"
  },
  "bugs": {
    "url": "https://github.com/FRRe-DS/2025-12-TPI/issues"
  },
  "homepage": "https://github.com/FRRe-DS/2025-12-TPI#readme",
  "keywords": [
    "nestjs",
    "typescript",
    "microservices",
    "logistics",
    "shipping",
    "prisma",
    "postgresql",
    "redis",
    "tpi",
    "utn",
    "frre"
  ],
  "scripts": {
    "install:all": "pnpm -r --filter \"./backend/**\" install && pnpm -C shared/database prisma:generate",
    "build:shared": "pnpm -C shared/database build && pnpm -C shared/types build && pnpm -C shared/utils build",
    "build:services": "pnpm -C services/config-service build && pnpm -C services/operator-interface-service build && pnpm -C services/shipping-service build && pnpm -C services/stock-integration-service build",
    "build": "pnpm run build:shared && pnpm run build:services",
    "start:config": "pnpm -C services/config-service start",
    "start:stock": "pnpm -C services/stock-integration-service start",
    "start:shipping": "pnpm -C services/shipping-service start",
    "start:operator": "pnpm -C services/operator-interface-service start",
    "start:all": "concurrently \"pnpm run start:config\" \"pnpm run start:stock\" \"pnpm run start:shipping\" \"pnpm run start:operator\"",
    "start:dev:config": "pnpm -C services/config-service start:dev",
    "start:dev:stock": "pnpm -C services/stock-integration-service start:dev",
    "start:dev:shipping": "pnpm -C services/shipping-service start:dev",
    "start:dev:operator": "pnpm -C services/operator-interface-service start:dev",
    "dev": "concurrently \"pnpm run start:dev:config\" \"pnpm run start:dev:stock\" \"pnpm run start:dev:shipping\" \"pnpm run start:dev:operator\"",
    "format": "prettier --write \"shared/**/*.ts\" \"services/**/*.ts\"",
    "lint": "eslint \"{shared,services}/**/*.ts\" --fix",
    "clean": "rimraf shared/*/node_modules shared/*/dist services/*/node_modules services/*/dist",
    "prisma:generate": "pnpm -C shared/database prisma:generate",
    "prisma:migrate": "pnpm -C shared/database prisma:migrate"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "concurrently": "^9.1.0",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.0.0",
    "eslint-plugin-prettier": "^5.0.0",
    "globals": "^15.0.0",
    "prettier": "^3.0.0",
    "rimraf": "^6.0.1",
    "typescript-eslint": "^8.0.0"
  }
}
